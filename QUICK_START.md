# دليل البدء السريع - Quick Start Guide

## 🚀 تشغيل النظام محلياً

### الطريقة الأولى: فتح مباشر
1. فتح ملف `index.html` في المتصفح
2. سيتم تحميل البيانات من Supabase تلقائياً

### الطريقة الثانية: خادم محلي
```bash
# Python 3
python -m http.server 8000

# أو Node.js (إذا كان مثبت)
npx http-server

# ثم فتح http://localhost:8000
```

## 📊 نقل البيانات إلى Supabase

### تلقائياً
1. فتح التطبيق
2. انتظار ظهور إشعار "تحديث النظام"
3. النقر على "ترقية الآن"
4. انتظار اكتمال النقل

### يدوياً
```javascript
// في console المتصفح (F12)
startMigration();
```

## 🌐 النشر على Netlify

### الطريقة السريعة
1. إنشاء مستودع GitHub جديد
2. رفع جميع الملفات
3. ربط المستودع بـ Netlify
4. النشر التلقائي

### خطوات مفصلة
```bash
# 1. إنشاء مستودع Git
git init
git add .
git commit -m "Initial commit"

# 2. ربط بـ GitHub
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main

# 3. في Netlify
# - New site from Git
# - اختيار المستودع
# - Deploy site
```

## ✅ اختبار النظام

### اختبارات أساسية
- [ ] تحميل البيانات من Supabase
- [ ] عرض العقارات في الجدول والبطاقات
- [ ] البحث والفلترة
- [ ] تصدير Excel
- [ ] رفع المرفقات

### اختبار التحديثات الفورية
1. فتح التطبيق في تبويبين
2. تعديل بيانات في أحدهما
3. مراقبة التحديث في الآخر

## 🔧 حل المشاكل الشائعة

### لا تظهر البيانات
```javascript
// التحقق من الاتصال
console.log('Supabase URL:', SUPABASE_URL);
console.log('Supabase Client:', supabaseClient);

// إعادة تحميل البيانات
loadAndDisplayProperties();
```

### خطأ في التحميل
1. التحقق من اتصال الإنترنت
2. التحقق من مفاتيح Supabase
3. مراجعة console للأخطاء

### مشاكل النشر
1. التأكد من ملف `netlify.toml`
2. التحقق من إعدادات البناء
3. مراجعة سجلات النشر

## 📱 الاستخدام

### عرض العقارات
- **المدن**: النقر على أزرار المدن في الأعلى
- **العقارات**: اختيار من القائمة الجانبية
- **العرض**: تبديل بين جدول وبطاقات

### البحث
- **بحث عام**: في مربع البحث الرئيسي
- **فلتر الحالة**: جاري، منتهي، على وشك، فارغ
- **فلتر التاريخ**: حسب تاريخ البداية أو النهاية

### إدارة المرفقات
- **رفع**: زر "إدارة المرفقات"
- **عرض**: النقر على أيقونة المرفق
- **حذف**: زر الحذف بجانب كل ملف

### التصدير
- **Excel**: زر "تصدير Excel"
- **مفلتر**: يصدر البيانات المعروضة حالياً

## 🔄 التحديثات الفورية

النظام يدعم التحديثات الفورية:
- تغيير البيانات يظهر فوراً لجميع المستخدمين
- لا حاجة لتحديث الصفحة
- إشعارات بالتغييرات الجديدة

## 📞 الدعم

### مشاكل تقنية
- مراجعة console المتصفح (F12)
- التحقق من سجلات Netlify
- مراجعة dashboard Supabase

### طلب مساعدة
- توثيق الخطأ بالصور
- نسخ رسائل الخطأ
- ذكر المتصفح ونظام التشغيل

---

## 🎯 نصائح للأداء الأمثل

1. **استخدام متصفح حديث** (Chrome, Firefox, Safari, Edge)
2. **اتصال إنترنت مستقر** للتحديثات الفورية
3. **تنظيف cache المتصفح** عند المشاكل
4. **تحديث البيانات دورياً** للحفاظ على الدقة

## 🔐 الأمان

- البيانات محمية بـ Row Level Security
- الاتصال مشفر (HTTPS)
- النسخ الاحتياطي التلقائي
- سجل جميع العمليات

---

**تم إعداد النظام بنجاح! 🎉**

للمزيد من التفاصيل، راجع ملف `README.md` أو `DEPLOYMENT.md`
